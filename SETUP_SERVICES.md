# راهنمای نصب و راه‌اندازی خدمات

## ✅ تغییرات انجام شده:

### 1. دکمه‌های صفحه اصلی
- ✅ دکمه "بیشتر بدانید" → لینک به `/about`
- ✅ دکمه "تماس با ما" → لینک به `/contact`

### 2. App جدید Services
- ✅ Model برای ذخیره خدمات
- ✅ Admin panel برای مدیریت
- ✅ API endpoint برای دریافت خدمات
- ✅ اسکریپت اضافه کردن خدمات اولیه

---

## 🚀 دستورات اجرا در سرور

### مرحله 1: ایجاد Migration

```bash
# ورود به کانتینر backend
docker exec -it irpps-backend bash

# ایجاد migration برای app جدید
python manage.py makemigrations services

# اجرای migration
python manage.py migrate

# خروج از کانتینر
exit
```

### مرحله 2: اضافه کردن خدمات به دیتابیس

```bash
# اجرای اسکریپت اضافه کردن خدمات
docker exec -it irpps-backend python /app/add_services.py
```

یا می‌تونی از اسکریپت آماده استفاده کنی:

```bash
cd /opt/irpps/src/backend
chmod +x run_add_services.sh
./run_add_services.sh
```

### مرحله 3: Rebuild و Restart

```bash
cd /opt/irpps/src

# Rebuild backend
docker-compose build backend

# Rebuild frontend
docker-compose build frontend

# Restart همه سرویس‌ها
docker-compose restart

# یا rebuild و restart همزمان
docker-compose up -d --build
```

---

## 📊 بررسی نتیجه

### 1. چک کردن API

```bash
# تست API خدمات
curl http://localhost:8000/api/services/

# یا از مرورگر
http://your-server-ip:8000/api/services/
```

### 2. چک کردن Admin Panel

1. برو به: `http://your-server-ip:8000/admin`
2. وارد شو با اکانت admin
3. بخش "خدمات" رو ببین
4. باید 6 خدمت اضافه شده باشه

### 3. چک کردن Frontend

1. برو به صفحه اصلی: `http://your-server-ip`
2. دکمه‌های "بیشتر بدانید" و "تماس با ما" رو تست کن
3. برو به صفحه Services: `http://your-server-ip/services`

---

## 📝 خدمات اضافه شده:

1. **مشاوره تخصصی** (medical_information)
   - ارائه مشاوره‌های علمی و تخصصی در زمینه بیماری‌های ریوی نوزادان و کودکان

2. **درمان‌های نوین** (healing)
   - معرفی و بکارگیری جدیدترین پروتکل‌های درمانی بین‌المللی برای بیماران تنفسی

3. **پیشگیری و سلامت** (health_and_safety)
   - برنامه‌های غربالگری و آگاهی‌رسانی جهت جلوگیری از بروز بیماری‌های مزمن ریوی

4. **آموزش مداوم** (school)
   - برگزاری کارگاه‌ها و کنفرانس‌های علمی برای پزشکان و متخصصان سراسر کشور

5. **پژوهش علمی** (biotech)
   - حمایت از پایان‌نامه‌ها و طرح‌های تحقیقاتی در حوزه تنفس و آسم کودکان

6. **حمایت از بیماران** (volunteer_activism)
   - ارائه خدمات حمایتی و مددکاری برای خانواده‌های دارای کودک با بیماری خاص

---

## 🔧 عیب‌یابی

### اگر خطای Migration گرفتی:

```bash
docker exec -it irpps-backend python manage.py migrate --fake-initial
```

### اگر خدمات اضافه نشدن:

```bash
# چک کردن لاگ
docker logs irpps-backend --tail 50

# اجرای مجدد اسکریپت
docker exec -it irpps-backend python /app/add_services.py
```

### اگر تغییرات Frontend نمایش داده نشد:

```bash
# پاک کردن cache مرورگر
# یا
docker-compose build --no-cache frontend
docker-compose up -d frontend
```

---

## ✨ نتیجه نهایی

بعد از اجرای این مراحل:
- ✅ دکمه‌های صفحه اصلی کار می‌کنن
- ✅ 6 خدمت در دیتابیس ذخیره شدن
- ✅ API خدمات در دسترسه
- ✅ Admin panel برای مدیریت خدمات آمادست
- ✅ صفحه Services با دیتای واقعی کار می‌کنه
