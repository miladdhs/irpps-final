# ✅ Dashboard Profile Editing - Complete

## تاریخ: 2026-02-10

## خلاصه تغییرات

صفحه Dashboard به‌طور کامل بازنویسی شد و قابلیت‌های ویرایش پروفایل اضافه شد.

---

## 🎯 قابلیت‌های جدید Dashboard

### 1. سیستم Tab-Based
صفحه به 3 تب تقسیم شده:
- **اطلاعات شخصی**: ویرایش نام، ایمیل، تلفن
- **عکس پروفایل**: آپلود و حذف عکس
- **رزومه**: ویرایش تحصیلات، مقالات، جوایز، و...

### 2. تب اطلاعات شخصی
**فیلدها:**
- نام فارسی (first_name)
- نام انگلیسی (last_name)
- ایمیل
- تلفن

**قابلیت‌ها:**
- فرم ویرایش با validation
- پیام‌های موفقیت/خطا
- به‌روزرسانی auth store بعد از ذخیره
- API: `PUT /api/accounts/update-profile/`

### 3. تب عکس پروفایل
**قابلیت‌ها:**
- نمایش عکس فعلی (یا placeholder)
- انتخاب فایل جدید با پیش‌نمایش
- آپلود عکس
- حذف عکس با تایید
- پیام‌های موفقیت/خطا
- فرمت‌های مجاز: JPG, PNG, GIF

**API Endpoints:**
- آپلود: `POST /api/accounts/upload-profile-image/`
- حذف: `DELETE /api/accounts/delete-profile-image/`

### 4. تب رزومه
**فیلدها:**
- تحصیلات (education)
- مقالات و انتشارات (publications)
- جوایز و افتخارات (awards)
- گواهینامه‌ها (certifications)
- علایق پژوهشی (research_interests)
- زبان‌ها (languages)
- بیوگرافی (bio)

**قابلیت‌ها:**
- فرم‌های textarea برای متن‌های بلند
- ذخیره تمام فیلدها با یک درخواست
- پیام‌های موفقیت/خطا
- API: `POST /api/accounts/update-resume/`

---

## 🔄 جریان کار (Workflow)

### ورود به Dashboard:
```
1. کاربر لاگین می‌کند
2. به /dashboard هدایت می‌شود
3. fetchProfile() اطلاعات کاربر را از API می‌گیرد
4. فرم‌ها با داده‌های موجود پر می‌شوند
```

### ویرایش اطلاعات شخصی:
```
1. کاربر تب "اطلاعات شخصی" را انتخاب می‌کند
2. فیلدها را ویرایش می‌کند
3. دکمه "ذخیره تغییرات" را می‌زند
4. updatePersonalInfo() داده‌ها را به API ارسال می‌کند
5. پیام موفقیت نمایش داده می‌شود
6. auth store به‌روزرسانی می‌شود
```

### آپلود عکس:
```
1. کاربر تب "عکس پروفایل" را انتخاب می‌کند
2. فایل را انتخاب می‌کند
3. پیش‌نمایش نمایش داده می‌شود
4. دکمه "آپلود عکس" را می‌زند
5. uploadProfileImage() فایل را به API ارسال می‌کند
6. عکس جدید در header نمایش داده می‌شود
```

### حذف عکس:
```
1. کاربر دکمه "حذف عکس" را می‌زند
2. تایید می‌کند
3. deleteProfileImage() درخواست حذف را ارسال می‌کند
4. عکس حذف می‌شود و placeholder نمایش داده می‌شود
```

### ویرایش رزومه:
```
1. کاربر تب "رزومه" را انتخاب می‌کند
2. فیلدهای مختلف را پر می‌کند
3. دکمه "ذخیره رزومه" را می‌زند
4. updateResume() تمام داده‌ها را به API ارسال می‌کند
5. پیام موفقیت نمایش داده می‌شود
```

---

## 📁 فایل‌های تغییر یافته

### Frontend:
- ✅ `frontend/src/views/Dashboard.vue` - بازنویسی کامل

### Backend (بدون تغییر):
- ✅ `backend/accounts/models.py` - مدل CustomUser
- ✅ `backend/accounts/views.py` - API endpoints
- ✅ `backend/accounts/urls.py` - URL routing

---

## 🔌 API Endpoints مورد استفاده

### 1. دریافت پروفایل
```
GET /api/accounts/profile/
Authorization: Required (Session)
```

### 2. به‌روزرسانی اطلاعات شخصی
```
PUT /api/accounts/update-profile/
Content-Type: application/json
Body: { first_name, last_name, email, phone }
```

### 3. آپلود عکس
```
POST /api/accounts/upload-profile-image/
Content-Type: multipart/form-data
Body: { profile_image: File }
```

### 4. حذف عکس
```
DELETE /api/accounts/delete-profile-image/
```

### 5. به‌روزرسانی رزومه
```
POST /api/accounts/update-resume/
Content-Type: application/json
Body: { education, publications, awards, certifications, research_interests, languages, bio }
```

---

## 🎨 طراحی UI/UX

### رنگ‌بندی:
- Primary: Blue-600
- Success: Green-600
- Error: Red-600
- Background: Gray-50

### المان‌های کلیدی:
- **Header**: نمایش عکس پروفایل، نام، ایمیل، کد ملی
- **Tabs**: سیستم tab با border-bottom برای tab فعال
- **Forms**: Input/Textarea با focus:ring-blue-500
- **Buttons**: 
  - Primary: bg-blue-600 hover:bg-blue-700
  - Danger: bg-red-600 hover:bg-red-700
- **Messages**: 
  - Success: bg-green-50 text-green-600
  - Error: bg-red-50 text-red-600

### Responsive:
- Mobile: تک ستونی
- Desktop: دو ستونی برای فرم اطلاعات شخصی
- Grid: 1/2/4 ستون برای Quick Links

---

## ✅ تست‌های لازم

### 1. تست اطلاعات شخصی:
- [ ] ویرایش نام فارسی
- [ ] ویرایش نام انگلیسی
- [ ] ویرایش ایمیل
- [ ] ویرایش تلفن
- [ ] بررسی پیام موفقیت
- [ ] بررسی به‌روزرسانی header

### 2. تست عکس پروفایل:
- [ ] انتخاب فایل و نمایش پیش‌نمایش
- [ ] آپلود عکس JPG
- [ ] آپلود عکس PNG
- [ ] حذف عکس
- [ ] بررسی نمایش در header
- [ ] بررسی نمایش در صفحه Team

### 3. تست رزومه:
- [ ] پر کردن تمام فیلدها
- [ ] ذخیره رزومه
- [ ] بررسی نمایش در صفحه Team (Modal)
- [ ] ویرایش و به‌روزرسانی

### 4. تست Navigation:
- [ ] تغییر بین تب‌ها
- [ ] کلیک روی Quick Links
- [ ] دکمه خروج

---

## 🚀 نحوه استفاده

### برای کاربر:

1. **ورود به سیستم:**
   ```
   - به صفحه Login بروید
   - کد ملی و کد نظام پزشکی را وارد کنید
   - وارد Dashboard می‌شوید
   ```

2. **ویرایش پروفایل:**
   ```
   - تب "اطلاعات شخصی" را انتخاب کنید
   - اطلاعات را ویرایش کنید
   - "ذخیره تغییرات" را بزنید
   ```

3. **آپلود عکس:**
   ```
   - تب "عکس پروفایل" را انتخاب کنید
   - "انتخاب عکس جدید" را بزنید
   - فایل را انتخاب کنید
   - "آپلود عکس" را بزنید
   ```

4. **ویرایش رزومه:**
   ```
   - تب "رزومه" را انتخاب کنید
   - فیلدها را پر کنید
   - "ذخیره رزومه" را بزنید
   ```

5. **مشاهده پروفایل عمومی:**
   ```
   - به صفحه "مشاهده اعضا" بروید
   - روی نام خود کلیک کنید
   - رزومه کامل در Modal نمایش داده می‌شود
   ```

---

## 📝 نکات مهم

### امنیت:
- تمام API ها نیاز به authentication دارند
- Session-based authentication
- CSRF protection فعال
- هر کاربر فقط پروفایل خودش را می‌تواند ویرایش کند

### عکس پروفایل:
- فقط فایل‌های image/* مجاز هستند
- عکس‌ها در `/media/profile_images/` ذخیره می‌شوند
- Nginx باید `/media/` را serve کند

### رزومه:
- تمام فیلدها اختیاری هستند
- می‌توانند خالی باشند
- در صفحه Team به صورت عمومی نمایش داده می‌شوند

---

## 🎉 وضعیت نهایی

- ✅ Dashboard کامل شد
- ✅ تمام فرم‌های ویرایش اضافه شدند
- ✅ اتصال به API های backend
- ✅ UI/UX مدرن و responsive
- ✅ پیام‌های موفقیت/خطا
- ✅ Validation و error handling
- ✅ Quick Links به صفحات دیگر

**سیستم پروفایل اعضا کاملاً آماده و قابل استفاده است!** 🚀

---

## 📞 پشتیبانی

در صورت بروز مشکل:
1. لاگ‌های browser console را بررسی کنید
2. لاگ‌های backend را بررسی کنید: `docker logs irpps-backend`
3. مطمئن شوید تمام container ها healthy هستند: `docker ps`

