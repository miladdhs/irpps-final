# 🚀 شروع سریع - دیزاین جدید

## دستورات سریع برای شروع کار:

### 1️⃣ نصب (یکبار):
```bash
cd frontend
npm install -D tailwindcss postcss autoprefixer
npm install
```

### 2️⃣ فعال‌سازی دیزاین جدید:

**Windows PowerShell:**
```powershell
.\install-new-design.ps1
```

**Linux/Mac:**
```bash
chmod +x install-new-design.sh
./install-new-design.sh
```

### 3️⃣ اجرا:
```bash
npm run dev
```

### 4️⃣ مشاهده:
مرورگر را باز کنید: `http://localhost:5173`

---

## 📋 چک‌لیست بررسی:

بعد از اجرا، این موارد را بررسی کنید:

- [ ] صفحه اصلی با دیزاین جدید نمایش داده می‌شود
- [ ] Header و Footer مدرن هستند
- [ ] منوی Navigation کار می‌کند
- [ ] صفحات About، News، Events، Services باز می‌شوند
- [ ] Responsive است (موبایل، تبلت، دسکتاپ)
- [ ] Material Icons نمایش داده می‌شوند
- [ ] فونت‌ها به درستی بارگذاری شده‌اند

---

## ❓ مشکل دارید?

### خطای "Cannot find module 'tailwindcss'":
```bash
npm install -D tailwindcss postcss autoprefixer
```

### صفحه سفید نمایش داده می‌شود:
```bash
# Console مرورگر را باز کنید (F12)
# خطاها را بررسی کنید
```

### استایل‌ها اعمال نمی‌شوند:
```bash
# مرورگر را Refresh کنید
Ctrl + F5  # یا Cmd + Shift + R در Mac
```

---

## 📚 مستندات کامل:

برای اطلاعات بیشتر، این فایل‌ها را مطالعه کنید:

1. **QUICK_START.md** - راهنمای سریع شروع
2. **README_FA.md** - راهنمای کامل فارسی
3. **NEW_DESIGN_MIGRATION.md** - راهنمای مهاجرت
4. **IMPLEMENTATION_SUMMARY.md** - خلاصه پیاده‌سازی

---

## 🎯 مراحل بعدی:

1. ✅ نصب و اجرا
2. ⏳ بررسی صفحات موجود
3. ⏳ تبدیل صفحات باقی‌مانده
4. ⏳ اضافه کردن محتوای واقعی
5. ⏳ تست کامل
6. ⏳ Deploy

---

**موفق باشید! 🎉**
