# استراکچر کامل خبر و اطلاعیه

این سند شامل توضیحات کامل و جامع ساختار مدل‌های **خبر (News)** و **اطلاعیه (Announcement)** در سیستم می‌باشد.

---

## 📰 ساختار مدل خبر (News Model)

### توضیحات کلی
مدل `News` برای ذخیره‌سازی و مدیریت اخبار در سیستم استفاده می‌شود. این مدل شامل فیلدهای مختلفی برای مدیریت کامل محتوا، دسته‌بندی، برچسب‌گذاری و آمار بازدید می‌باشد.

---

### فیلدهای مدل News

#### 1. **id** (شناسه)
- **نوع:** `BigAutoField` (عدد صحیح بزرگ - خودکار)
- **توضیحات:** شناسه یکتا و اصلی هر خبر که به صورت خودکار توسط دیتابیس تولید می‌شود
- **اجباری:** بله (خودکار)
- **قابل ویرایش:** خیر (فقط خواندنی)

---

#### 2. **title** (عنوان)
- **نوع:** `CharField` (رشته متنی)
- **حداکثر طول:** 200 کاراکتر
- **توضیحات:** عنوان اصلی خبر که باید واضح، جذاب و توصیفی باشد
- **اجباری:** بله
- **مثال:** "برگزاری همایش بین‌المللی امنیت سایبری 2025"

---

#### 3. **slug** (اسلاگ)
- **نوع:** `SlugField` (رشته متنی URL-friendly)
- **حداکثر طول:** 200 کاراکتر
- **ویژگی:** یکتا (unique)
- **توضیحات:** آدرس URL دوستانه برای دسترسی به خبر. معمولاً از عنوان خبر به صورت خودکار تولید می‌شود
- **اجباری:** بله
- **مثال:** "hamayesh-amniat-sayberi-2025"
- **نکته:** فقط شامل حروف انگلیسی، اعداد و خط تیره (-) می‌باشد

---

#### 4. **content** (محتوا)
- **نوع:** `TextField` (متن طولانی)
- **حداکثر طول:** نامحدود
- **توضیحات:** محتوای کامل و اصلی خبر که شامل تمام جزئیات، توضیحات و اطلاعات خبر می‌باشد
- **اجباری:** بله
- **مثال:** متن کامل خبر با تمام جزئیات، پاراگراف‌ها و اطلاعات

---

#### 5. **short_content** (خلاصه خبر)
- **نوع:** `TextField` (متن)
- **حداکثر طول:** 500 کاراکتر
- **توضیحات:** خلاصه کوتاه خبر که برای نمایش در لیست اخبار و پیش‌نمایش استفاده می‌شود
- **اجباری:** خیر (اختیاری)
- **پیش‌فرض:** خالی (null/blank)
- **مثال:** "همایش بین‌المللی امنیت سایبری در تاریخ 15 دی ماه 1403 برگزار می‌شود..."

---

#### 6. **image** (تصویر)
- **نوع:** `ImageField` (فیلد تصویر)
- **مسیر آپلود:** `news/` (در پوشه media)
- **توضیحات:** تصویر اصلی خبر که برای نمایش در لیست و صفحه جزئیات استفاده می‌شود
- **اجباری:** خیر (اختیاری)
- **پیش‌فرض:** خالی (null/blank)
- **فرمت‌های پشتیبانی شده:** JPG, PNG, GIF, WebP و سایر فرمت‌های تصویری
- **نکته:** تصویر در پوشه `media/news/` ذخیره می‌شود

---

#### 7. **author** (نویسنده)
- **نوع:** `ForeignKey` (ارتباط با مدل User)
- **مدل مرتبط:** `User` (مدل کاربر)
- **on_delete:** `CASCADE` (با حذف کاربر، اخبار او نیز حذف می‌شوند)
- **related_name:** `news` (دسترسی معکوس: `user.news.all()`)
- **توضیحات:** کاربری که خبر را ایجاد کرده است
- **اجباری:** بله
- **مثال:** ارتباط با کاربر با شناسه 1

---

#### 8. **category** (دسته‌بندی)
- **نوع:** `CharField` (رشته متنی)
- **حداکثر طول:** 100 کاراکتر
- **توضیحات:** دسته‌بندی یا موضوع خبر برای سازماندهی بهتر
- **اجباری:** خیر (اختیاری)
- **پیش‌فرض:** خالی (null/blank)
- **مثال:** "امنیت سایبری", "همایش", "آموزش", "فناوری"

---

#### 9. **tags** (برچسب‌ها)
- **نوع:** `CharField` (رشته متنی)
- **حداکثر طول:** 300 کاراکتر
- **توضیحات:** برچسب‌های مرتبط با خبر که با کاما (,) از هم جدا می‌شوند
- **اجباری:** خیر (اختیاری)
- **پیش‌فرض:** خالی (null/blank)
- **فرمت:** برچسب‌ها با کاما جدا می‌شوند
- **مثال:** "امنیت,سایبری,همایش,آموزش"

---

#### 10. **source** (منبع)
- **نوع:** `CharField` (رشته متنی)
- **حداکثر طول:** 200 کاراکتر
- **توضیحات:** منبع خبر در صورت استفاده از منابع خارجی
- **اجباری:** خیر (اختیاری)
- **پیش‌فرض:** خالی (null/blank)
- **مثال:** "خبرگزاری ایسنا", "وب‌سایت رسمی", "دپارتمان IT"

---

#### 11. **is_published** (منتشر شده)
- **نوع:** `BooleanField` (بولی - درست/غلط)
- **پیش‌فرض:** `True` (منتشر شده)
- **توضیحات:** تعیین می‌کند که خبر منتشر شده است یا خیر. فقط اخبار منتشر شده برای کاربران عادی نمایش داده می‌شوند
- **اجباری:** بله (با پیش‌فرض)
- **مقادیر:** `True` (منتشر شده) یا `False` (پیش‌نویس)

---

#### 12. **created_at** (تاریخ ایجاد)
- **نوع:** `DateTimeField` (تاریخ و زمان)
- **ویژگی:** `auto_now_add=True` (خودکار در زمان ایجاد)
- **توضیحات:** تاریخ و زمان ایجاد خبر که به صورت خودکار تنظیم می‌شود
- **اجباری:** بله (خودکار)
- **قابل ویرایش:** خیر (فقط خواندنی)
- **فرمت:** ISO 8601 (مثال: 2025-01-15T10:30:00)

---

#### 13. **updated_at** (تاریخ بروزرسانی)
- **نوع:** `DateTimeField` (تاریخ و زمان)
- **ویژگی:** `auto_now=True` (خودکار در هر بروزرسانی)
- **توضیحات:** تاریخ و زمان آخرین بروزرسانی خبر که به صورت خودکار در هر تغییر به‌روز می‌شود
- **اجباری:** بله (خودکار)
- **قابل ویرایش:** خیر (فقط خواندنی)
- **فرمت:** ISO 8601

---

#### 14. **views** (تعداد بازدید)
- **نوع:** `IntegerField` (عدد صحیح)
- **پیش‌فرض:** 0
- **توضیحات:** تعداد دفعاتی که خبر مشاهده شده است. این مقدار به صورت خودکار با هر بازدید افزایش می‌یابد
- **اجباری:** بله (با پیش‌فرض)
- **قابل ویرایش:** بله (اما معمولاً به صورت خودکار مدیریت می‌شود)

---

### تنظیمات Meta مدل News

```python
class Meta:
    verbose_name = 'خبر'              # نام تک در پنل ادمین
    verbose_name_plural = 'اخبار'     # نام جمع در پنل ادمین
    ordering = ['-created_at']         # مرتب‌سازی بر اساس تاریخ ایجاد (جدیدترین اول)
```

---

## 📢 ساختار مدل اطلاعیه (Announcement Model)

### توضیحات کلی
مدل `Announcement` برای ذخیره‌سازی و مدیریت اطلاعیه‌ها در سیستم استفاده می‌شود. اطلاعیه‌ها معمولاً کوتاه‌تر از اخبار هستند و برای اعلان‌های مهم و فوری استفاده می‌شوند.

---

### فیلدهای مدل Announcement

#### 1. **id** (شناسه)
- **نوع:** `BigAutoField` (عدد صحیح بزرگ - خودکار)
- **توضیحات:** شناسه یکتا و اصلی هر اطلاعیه
- **اجباری:** بله (خودکار)
- **قابل ویرایش:** خیر

---

#### 2. **title** (عنوان)
- **نوع:** `CharField` (رشته متنی)
- **حداکثر طول:** 200 کاراکتر
- **توضیحات:** عنوان اصلی اطلاعیه
- **اجباری:** بله
- **مثال:** "تغییر زمان برگزاری جلسه"

---

#### 3. **slug** (اسلاگ)
- **نوع:** `SlugField` (رشته متنی URL-friendly)
- **حداکثر طول:** 200 کاراکتر
- **ویژگی:** یکتا (unique)
- **توضیحات:** آدرس URL دوستانه برای دسترسی به اطلاعیه
- **اجباری:** بله
- **مثال:** "taghir-zaman-jalase"

---

#### 4. **content** (محتوا)
- **نوع:** `TextField` (متن طولانی)
- **حداکثر طول:** نامحدود
- **توضیحات:** محتوای کامل اطلاعیه
- **اجباری:** بله
- **مثال:** متن کامل اطلاعیه با تمام جزئیات

---

#### 5. **image** (تصویر)
- **نوع:** `ImageField` (فیلد تصویر)
- **مسیر آپلود:** `announcements/` (در پوشه media)
- **توضیحات:** تصویر اطلاعیه (اختیاری)
- **اجباری:** خیر (اختیاری)
- **پیش‌فرض:** خالی (null/blank)
- **نکته:** تصویر در پوشه `media/announcements/` ذخیره می‌شود

---

#### 6. **author** (نویسنده)
- **نوع:** `ForeignKey` (ارتباط با مدل User)
- **مدل مرتبط:** `User`
- **on_delete:** `CASCADE`
- **related_name:** `announcements` (دسترسی معکوس: `user.announcements.all()`)
- **توضیحات:** کاربری که اطلاعیه را ایجاد کرده است
- **اجباری:** بله

---

#### 7. **is_published** (منتشر شده)
- **نوع:** `BooleanField` (بولی)
- **پیش‌فرض:** `True` (منتشر شده)
- **توضیحات:** تعیین می‌کند که اطلاعیه منتشر شده است یا خیر
- **اجباری:** بله (با پیش‌فرض)

---

#### 8. **is_important** (مهم)
- **نوع:** `BooleanField` (بولی)
- **پیش‌فرض:** `False` (غیر مهم)
- **توضیحات:** تعیین می‌کند که اطلاعیه مهم است یا خیر. اطلاعیه‌های مهم معمولاً با استایل متفاوت یا در بالای لیست نمایش داده می‌شوند
- **اجباری:** بله (با پیش‌فرض)
- **مقادیر:** `True` (مهم) یا `False` (عادی)

---

#### 9. **created_at** (تاریخ ایجاد)
- **نوع:** `DateTimeField` (تاریخ و زمان)
- **ویژگی:** `auto_now_add=True` (خودکار در زمان ایجاد)
- **توضیحات:** تاریخ و زمان ایجاد اطلاعیه
- **اجباری:** بله (خودکار)
- **قابل ویرایش:** خیر

---

#### 10. **updated_at** (تاریخ بروزرسانی)
- **نوع:** `DateTimeField` (تاریخ و زمان)
- **ویژگی:** `auto_now=True` (خودکار در هر بروزرسانی)
- **توضیحات:** تاریخ و زمان آخرین بروزرسانی اطلاعیه
- **اجباری:** بله (خودکار)
- **قابل ویرایش:** خیر

---

#### 11. **views** (تعداد بازدید)
- **نوع:** `IntegerField` (عدد صحیح)
- **پیش‌فرض:** 0
- **توضیحات:** تعداد دفعاتی که اطلاعیه مشاهده شده است
- **اجباری:** بله (با پیش‌فرض)

---

### تنظیمات Meta مدل Announcement

```python
class Meta:
    verbose_name = 'اطلاعیه'              # نام تک در پنل ادمین
    verbose_name_plural = 'اطلاعیه‌ها'    # نام جمع در پنل ادمین
    ordering = ['-created_at']            # مرتب‌سازی بر اساس تاریخ ایجاد (جدیدترین اول)
```

---

## 🔄 تفاوت‌های کلیدی بین News و Announcement

| ویژگی | News (خبر) | Announcement (اطلاعیه) |
|-------|------------|----------------------|
| **short_content** | ✅ دارد | ❌ ندارد |
| **category** | ✅ دارد | ❌ ندارد |
| **tags** | ✅ دارد | ❌ ندارد |
| **source** | ✅ دارد | ❌ ندارد |
| **is_important** | ❌ ندارد | ✅ دارد |
| **مسیر تصویر** | `news/` | `announcements/` |
| **related_name** | `news` | `announcements` |

---

## 📋 خلاصه فیلدهای اجباری و اختیاری

### مدل News (خبر)

**فیلدهای اجباری:**
- `title` (عنوان)
- `slug` (اسلاگ)
- `content` (محتوا)
- `author` (نویسنده)

**فیلدهای اختیاری:**
- `short_content` (خلاصه خبر)
- `image` (تصویر)
- `category` (دسته‌بندی)
- `tags` (برچسب‌ها)
- `source` (منبع)

**فیلدهای خودکار:**
- `id` (شناسه)
- `created_at` (تاریخ ایجاد)
- `updated_at` (تاریخ بروزرسانی)
- `views` (تعداد بازدید - با پیش‌فرض 0)
- `is_published` (منتشر شده - با پیش‌فرض True)

---

### مدل Announcement (اطلاعیه)

**فیلدهای اجباری:**
- `title` (عنوان)
- `slug` (اسلاگ)
- `content` (محتوا)
- `author` (نویسنده)

**فیلدهای اختیاری:**
- `image` (تصویر)

**فیلدهای خودکار:**
- `id` (شناسه)
- `created_at` (تاریخ ایجاد)
- `updated_at` (تاریخ بروزرسانی)
- `views` (تعداد بازدید - با پیش‌فرض 0)
- `is_published` (منتشر شده - با پیش‌فرض True)
- `is_important` (مهم - با پیش‌فرض False)

---

## 🔐 دسترسی و مجوزها

### دسترسی به API

**خواندن (GET):**
- کاربران عادی: فقط اخبار/اطلاعیه‌های منتشر شده (`is_published=True`)
- کاربران Staff: تمام اخبار/اطلاعیه‌ها (منتشر شده و منتشر نشده)

**ایجاد (POST):**
- فقط کاربران Staff

**ویرایش (PUT):**
- فقط کاربران Staff

**حذف (DELETE):**
- فقط کاربران Staff

---

## 📊 مثال JSON برای ایجاد خبر

```json
{
  "title": "برگزاری همایش بین‌المللی امنیت سایبری",
  "slug": "hamayesh-amniat-sayberi-2025",
  "content": "همایش بین‌المللی امنیت سایبری در تاریخ 15 دی ماه 1403 در محل سالن همایش‌های بین‌المللی تهران برگزار می‌شود. این همایش با حضور متخصصان برجسته داخلی و خارجی...",
  "short_content": "همایش بین‌المللی امنیت سایبری در تاریخ 15 دی ماه 1403 برگزار می‌شود.",
  "category": "همایش",
  "tags": "امنیت,سایبری,همایش,آموزش",
  "source": "دپارتمان IT",
  "is_published": true
}
```

---

## 📊 مثال JSON برای ایجاد اطلاعیه

```json
{
  "title": "تغییر زمان برگزاری جلسه",
  "slug": "taghir-zaman-jalase",
  "content": "به اطلاع می‌رساند زمان برگزاری جلسه هفتگی از ساعت 10 صبح به ساعت 14 بعدازظهر تغییر یافته است.",
  "is_published": true,
  "is_important": true
}
```

---

## 🎯 نکات مهم و بهترین روش‌ها

### برای News (خبر):

1. **عنوان:** باید واضح، جذاب و توصیفی باشد
2. **اسلاگ:** باید یکتا باشد و از کاراکترهای URL-safe استفاده کند
3. **محتوا:** باید کامل و جامع باشد
4. **خلاصه خبر:** بهتر است بین 100 تا 500 کاراکتر باشد
5. **دسته‌بندی:** استفاده از دسته‌بندی‌های ثابت برای سازماندهی بهتر
6. **برچسب‌ها:** استفاده از برچسب‌های مرتبط برای جستجوی بهتر
7. **منبع:** در صورت استفاده از منابع خارجی، حتماً ذکر شود

### برای Announcement (اطلاعیه):

1. **عنوان:** باید کوتاه و واضح باشد
2. **محتوا:** معمولاً کوتاه‌تر از خبر است
3. **is_important:** برای اطلاعیه‌های فوری و مهم استفاده شود
4. **اسلاگ:** باید یکتا باشد

---

## 📁 ساختار فایل‌ها در سیستم

```
backend/
  news/
    models.py          # تعریف مدل‌های News و Announcement
    admin.py           # تنظیمات پنل ادمین
    views.py           # View های API
    urls.py            # مسیرهای URL
    migrations/        # فایل‌های مایگریشن دیتابیس
```

---

## 🔍 جستجو و فیلتر

### فیلدهای قابل جستجو در News:
- `title` (عنوان)
- `content` (محتوا)
- `short_content` (خلاصه خبر)
- `author__username` (نام کاربری نویسنده)
- `category` (دسته‌بندی)
- `tags` (برچسب‌ها)
- `source` (منبع)

### فیلدهای قابل جستجو در Announcement:
- `title` (عنوان)
- `content` (محتوا)
- `author__username` (نام کاربری نویسنده)

### فیلترهای موجود:
- **News:** `is_published`, `category`, `created_at`
- **Announcement:** `is_published`, `is_important`, `created_at`

---

## 📝 خلاصه نهایی

### مدل News شامل:
- ✅ 14 فیلد (4 اجباری، 5 اختیاری، 5 خودکار)
- ✅ پشتیبانی از دسته‌بندی و برچسب‌گذاری
- ✅ خلاصه خبر برای پیش‌نمایش
- ✅ منبع خبر

### مدل Announcement شامل:
- ✅ 11 فیلد (4 اجباری، 1 اختیاری، 6 خودکار)
- ✅ فیلد مهم (is_important) برای اطلاعیه‌های فوری
- ✅ ساختار ساده‌تر نسبت به خبر

---

**تاریخ ایجاد سند:** 1403/10/25  
**نسخه:** 1.0  
**وضعیت:** کامل و به‌روز

