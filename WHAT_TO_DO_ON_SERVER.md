# دستورالعمل اجرا روی سرور

## خلاصه تغییرات

من فایل‌های زیر رو تغییر دادم یا اضافه کردم:

### فایل‌های Backend (Python/Django):
1. `backend/news/views.py` - اضافه کردن APIهای ویرایش و حذف اطلاعیه
2. `backend/news/urls.py` - اضافه کردن URLهای جدید برای اطلاعیه‌ها
3. `backend/events/views.py` - اضافه کردن APIهای ویرایش و حذف رویداد
4. `backend/events/urls.py` - اضافه کردن URLهای جدید برای رویدادها
5. `backend/make_user_staff_server.sh` - اسکریپت برای تبدیل یوزر به staff

### فایل‌های Frontend (Vue.js):
1. `frontend/src/views/admin/ManageAnnouncements.vue` - صفحه مدیریت اطلاعیه‌ها (جدید)
2. `frontend/src/views/admin/ManageNews.vue` - بروزرسانی
3. `frontend/src/views/admin/ManageEvents.vue` - بروزرسانی
4. `frontend/src/views/admin/AdminDashboard.vue` - اضافه کردن لینک اطلاعیه‌ها
5. `frontend/src/router/index.ts` - اضافه کردن route برای اطلاعیه‌ها

### فایل‌های راهنما:
1. `ADMIN_PANEL_SETUP.md` - راهنمای کامل فعال‌سازی پنل مدیریت
2. `WHAT_TO_DO_ON_SERVER.md` - این فایل

---

## مراحل اجرا روی سرور

### مرحله 1: آپلود فایل‌ها به سرور

فایل‌های تغییر یافته رو به سرور آپلود کنید. می‌تونید از Git یا FTP استفاده کنید:

#### روش 1: استفاده از Git (توصیه می‌شه)
```bash
# روی کامپیوتر محلی
git add .
git commit -m "Add admin panel for news, announcements and events"
git push origin main

# روی سرور
cd /path/to/your/project
git pull origin main
```

#### روش 2: استفاده از SCP/FTP
فایل‌های زیر رو به سرور کپی کنید:
- تمام فایل‌های `backend/` که تغییر کردن
- تمام فایل‌های `frontend/` که تغییر کردن

---

### مرحله 2: فعال‌سازی یوزر Admin

روی سرور، به پوشه backend بروید و اسکریپت رو اجرا کنید:

```bash
cd /path/to/your/project/backend

# اجرایی کردن اسکریپت
chmod +x make_user_staff_server.sh

# اجرای اسکریپت
./make_user_staff_server.sh admin
```

اگر اسکریپت کار نکرد، می‌تونید دستی انجام بدید:

```bash
python3 manage.py shell
```

بعد این کدها رو وارد کنید:

```python
from django.contrib.auth import get_user_model
User = get_user_model()
user = User.objects.get(username='admin')
user.is_staff = True
user.is_active = True
user.save()
print(f"✓ User {user.username} is now staff!")
exit()
```

---

### مرحله 3: بیلد کردن Frontend

اگر از Docker استفاده می‌کنید:

```bash
cd /path/to/your/project

# ری‌استارت کردن کانتینرها
docker-compose down
docker-compose up -d --build
```

اگر بدون Docker هستید:

```bash
cd /path/to/your/project/frontend

# نصب وابستگی‌ها (اگر لازم باشه)
npm install

# بیلد کردن
npm run build
```

---

### مرحله 4: ری‌استارت کردن Backend

اگر از Docker استفاده می‌کنید، مرحله 3 کافیه.

اگر بدون Docker هستید:

```bash
# اگر از systemd استفاده می‌کنید
sudo systemctl restart your-django-service

# یا اگر از supervisor استفاده می‌کنید
sudo supervisorctl restart your-django-app

# یا اگر دستی اجرا می‌کنید
# پروسس قبلی رو kill کنید و دوباره اجرا کنید
pkill -f "python manage.py runserver"
python3 manage.py runserver 0.0.0.0:8000
```

---

### مرحله 5: تست کردن

1. از سایت خارج شوید (Logout)
2. دوباره با یوزر `admin` وارد شوید
3. باید این بخش‌ها رو ببینید:
   - **پنل مدیریت** در منوی کاربری
   - **مدیریت اخبار**: `/admin/news`
   - **مدیریت اطلاعیه‌ها**: `/admin/announcements`
   - **مدیریت رویدادها**: `/admin/events`
   - **مدیریت اعضا**: `/admin/members`

---

## عیب‌یابی

### اگر پنل مدیریت رو نمی‌بینید:

1. مطمئن شوید که یوزر staff شده:
```bash
python3 manage.py shell
from django.contrib.auth import get_user_model
User = get_user_model()
user = User.objects.get(username='admin')
print(f"is_staff: {user.is_staff}")
print(f"is_active: {user.is_active}")
```

2. از سایت خارج شوید و دوباره وارد شوید
3. Cache مرورگر رو پاک کنید (Ctrl+Shift+Delete)

### اگر خطای 404 می‌گیرید:

مطمئن شوید که:
- فایل‌های backend به درستی آپلود شدن
- Django ری‌استارت شده
- URLها درست تنظیم شدن

### اگر خطای Permission Denied می‌گیرید:

```bash
# بررسی دسترسی‌های فایل
ls -la backend/make_user_staff_server.sh

# اگر لازم بود، دسترسی بدید
chmod +x backend/make_user_staff_server.sh
```

---

## لاگ‌ها

برای دیدن لاگ‌ها:

```bash
# اگر از Docker استفاده می‌کنید
docker logs irpps-backend -f

# اگر بدون Docker هستید
tail -f backend/logs/django.log

# یا لاگ‌های nginx
tail -f /var/log/nginx/error.log
```

---

## پشتیبانی

اگر مشکلی پیش اومد:
1. لاگ‌های سرور رو بررسی کنید
2. مطمئن شوید تمام فایل‌ها آپلود شدن
3. مطمئن شوید Django و Frontend ری‌استارت شدن
4. Cache مرورگر رو پاک کنید

---

## چک‌لیست نهایی

- [ ] فایل‌ها به سرور آپلود شدن
- [ ] یوزر admin به staff تبدیل شد
- [ ] Frontend بیلد شد
- [ ] Backend ری‌استارت شد
- [ ] از سایت خارج شدم و دوباره وارد شدم
- [ ] پنل مدیریت رو می‌بینم
- [ ] می‌تونم خبر جدید اضافه کنم
- [ ] می‌تونم اطلاعیه جدید اضافه کنم
- [ ] می‌تونم رویداد جدید اضافه کنم

موفق باشید! 🎉
