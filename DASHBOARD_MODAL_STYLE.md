# ✅ Dashboard با سبک Modal (مثل نسخه قبلی)

## تاریخ: 2026-02-10

---

## 🎯 تغییرات انجام شده

Dashboard به سبک قبلی (با Modal ها) بازنویسی شد تا کاربر با رابط آشنا باشد.

---

## 📋 ساختار جدید Dashboard

### 1. **Header (بالای صفحه)**
- نمایش عکس پروفایل یا Avatar
- نام کاربر و ایمیل
- کد ملی
- دکمه خروج

### 2. **بخش اصلی (Main Content)**

#### **ستون چپ (8 ستون):**

**الف) فرم ویرایش اطلاعات شخصی (Inline Toggle)**
- با کلیک روی دکمه "ویرایش"، فرم باز می‌شود
- فیلدها: نام فارسی، نام انگلیسی، ایمیل، تلفن
- دکمه‌ها: "ذخیره تغییرات" و "انصراف"
- پیام موفقیت/خطا
- بعد از ذخیره موفق، فرم بسته می‌شود

**ب) نمایش اطلاعات حساب کاربری**
- نمایش اطلاعات فعلی با آیکون
- دکمه "ویرایش" برای باز کردن فرم

#### **ستون راست (4 ستون - Sidebar):**

**الف) نمایش عکس پروفایل**
- عکس پروفایل یا Avatar
- نام و نام کاربری

**ب) دکمه‌های عملیاتی:**
1. **افزودن/تغییر عکس** → باز کردن Modal آپلود عکس
2. **حذف عکس** (فقط اگر عکس وجود داشته باشد)
3. **رزومه و توضیحات** → باز کردن Modal رزومه

---

## 🪟 Modal ها

### 1. **Modal آپلود عکس**

**نحوه باز شدن:**
```javascript
showImageUploadModal.value = true
```

**محتوا:**
- عنوان: "تغییر عکس پروفایل"
- فیلد انتخاب فایل (فقط image/*)
- پیش‌نمایش عکس انتخاب شده
- دکمه "آپلود عکس"
- پیام موفقیت/خطا

**عملکرد:**
1. کاربر فایل را انتخاب می‌کند
2. پیش‌نمایش نمایش داده می‌شود
3. با کلیک روی "آپلود"، عکس به سرور ارسال می‌شود
4. بعد از موفقیت، Modal بسته می‌شود و عکس جدید نمایش داده می‌شود

**API:**
```
POST /api/accounts/upload-profile-image/
Content-Type: multipart/form-data
Body: { profile_image: File }
```

---

### 2. **Modal رزومه**

**نحوه باز شدن:**
```javascript
showResumeModal.value = true
```

**محتوا:**
- عنوان: "توضیحات عمومی و رزومه"
- 7 فیلد:
  1. توضیحات عمومی (bio)
  2. تحصیلات (education)
  3. مقالات و انتشارات (publications)
  4. جوایز و افتخارات (awards)
  5. گواهینامه‌ها (certifications)
  6. علایق پژوهشی (research_interests)
  7. زبان‌ها (languages)
- دکمه "ذخیره رزومه"
- پیام موفقیت/خطا

**عملکرد:**
1. کاربر فیلدها را پر می‌کند
2. با کلیک روی "ذخیره"، تمام داده‌ها به سرور ارسال می‌شوند
3. بعد از موفقیت، Modal بسته می‌شود

**API:**
```
POST /api/accounts/update-resume/
Content-Type: application/json
Body: { bio, education, publications, awards, certifications, research_interests, languages }
```

---

## 🔄 جریان کار (Workflow)

### **ویرایش اطلاعات شخصی:**
```
1. کلیک روی دکمه "ویرایش"
2. فرم inline باز می‌شود
3. کاربر اطلاعات را ویرایش می‌کند
4. کلیک روی "ذخیره تغییرات"
5. پیام موفقیت نمایش داده می‌شود
6. فرم بسته می‌شود
```

### **آپلود عکس:**
```
1. کلیک روی دکمه "افزودن عکس" یا "تغییر عکس"
2. Modal باز می‌شود
3. انتخاب فایل
4. پیش‌نمایش نمایش داده می‌شود
5. کلیک روی "آپلود عکس"
6. پیام موفقیت نمایش داده می‌شود
7. Modal بسته می‌شود
8. عکس جدید در header و sidebar نمایش داده می‌شود
```

### **حذف عکس:**
```
1. کلیک روی دکمه "حذف عکس"
2. تایید حذف
3. عکس حذف می‌شود
4. Avatar پیش‌فرض نمایش داده می‌شود
```

### **ویرایش رزومه:**
```
1. کلیک روی دکمه "رزومه و توضیحات"
2. Modal باز می‌شود
3. کاربر فیلدها را پر می‌کند
4. کلیک روی "ذخیره رزومه"
5. پیام موفقیت نمایش داده می‌شود
6. Modal بسته می‌شود
```

---

## 🎨 طراحی UI/UX

### **رنگ‌بندی:**
- Primary: Blue (آبی)
- Success: Green (سبز)
- Danger: Red (قرمز)
- Background: Gray-50

### **المان‌های کلیدی:**
- **Header**: عکس پروفایل، نام، ایمیل، کد ملی، دکمه خروج
- **Inline Form**: فرم ویرایش با انیمیشن fade-slide
- **Modals**: 
  - پس‌زمینه تیره (bg-black/50)
  - محتوای سفید با border-radius گرد
  - دکمه بستن در گوشه
  - کلیک روی پس‌زمینه → بستن Modal
- **Buttons**:
  - Primary: bg-blue-600
  - Success: bg-green-600
  - Danger: bg-red-600
  - Soft: bg-blue-50 text-blue-600
- **Icons**: Material Symbols Outlined

### **Responsive:**
- Mobile: تک ستونی
- Desktop: دو ستونی (8+4)
- Modals: تمام صفحه در موبایل، مرکز صفحه در دسکتاپ

---

## ✅ مزایای سبک Modal

1. **آشنا برای کاربر**: مثل نسخه قبلی کار می‌کند
2. **تمرکز بهتر**: هر عملیات در یک Modal جداگانه
3. **کمتر گیج‌کننده**: بدون Tab switching
4. **فضای بیشتر**: Modal ها فضای کامل صفحه را استفاده می‌کنند
5. **انیمیشن‌های روان**: باز و بسته شدن با انیمیشن

---

## 📝 تفاوت با نسخه Tab-based قبلی

| ویژگی | نسخه Tab-based | نسخه Modal-based (فعلی) |
|-------|---------------|------------------------|
| ویرایش اطلاعات | Tab جداگانه | Inline form |
| آپلود عکس | Tab جداگانه | Modal |
| ویرایش رزومه | Tab جداگانه | Modal |
| تعداد کلیک | کمتر (یک کلیک) | بیشتر (باز/بسته کردن Modal) |
| فضای صفحه | همه چیز در یک صفحه | Modal ها روی صفحه باز می‌شوند |
| آشنایی کاربر | جدید | مثل نسخه قبلی |

---

## 🚀 نحوه استفاده

### **برای کاربر:**

1. **ویرایش اطلاعات:**
   - کلیک روی "ویرایش"
   - فیلدها را پر کنید
   - "ذخیره تغییرات" را بزنید

2. **آپلود عکس:**
   - کلیک روی "افزودن عکس" یا "تغییر عکس"
   - فایل را انتخاب کنید
   - "آپلود عکس" را بزنید

3. **ویرایش رزومه:**
   - کلیک روی "رزومه و توضیحات"
   - فیلدها را پر کنید
   - "ذخیره رزومه" را بزنید

4. **حذف عکس:**
   - کلیک روی "حذف عکس"
   - تایید کنید

---

## 📁 فایل‌های مرتبط

- ✅ `frontend/src/views/Dashboard.vue` - Dashboard جدید با Modal ها
- ✅ `backend/accounts/views.py` - API endpoints (بدون تغییر)
- ✅ `backend/accounts/models.py` - مدل CustomUser (بدون تغییر)

---

## 🎉 وضعیت نهایی

- ✅ Dashboard با سبک Modal (مثل نسخه قبلی)
- ✅ فرم inline برای ویرایش اطلاعات شخصی
- ✅ Modal برای آپلود عکس
- ✅ Modal برای ویرایش رزومه
- ✅ حذف عکس با تایید
- ✅ پیام‌های موفقیت/خطا
- ✅ انیمیشن‌های روان
- ✅ Responsive design

**سیستم کاملاً آماده و مثل نسخه قبلی کار می‌کند!** 🚀

