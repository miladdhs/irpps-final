# 🔗 راهنمای اتصال فرانت به بکند

## خلاصه

تمام تنظیمات لازم برای اتصال فرانت به بکند و دیتابیس انجام شده است.

## 🎯 چه کاری انجام شد؟

### 1. تنظیمات Frontend

✅ فایل `frontend/vite.config.ts` بروزرسانی شد
- Proxy برای development اضافه شد
- تنظیمات build برای production بهینه شد

✅ فایل `frontend/.env.production` ایجاد شد
- API URL برای production تنظیم شد

✅ فایل `frontend/src/services/api.ts` بروزرسانی شد
- Base URL به `/api` تغییر کرد
- Cookies و CSRF token به درستی handle می‌شوند

### 2. تنظیمات Backend

✅ همه چیز از قبل آماده بود:
- CORS تنظیم شده
- CSRF تنظیم شده
- Cookie settings صحیح
- Database به MySQL متصل
- API endpoints کامل

### 3. تنظیمات Nginx

✅ همه چیز از قبل آماده بود:
- Proxy به backend برای `/api/`
- Proxy به backend برای `/media/`
- CORS headers
- SPA fallback

### 4. تنظیمات Docker

✅ همه چیز از قبل آماده بود:
- Frontend container (Vue + Nginx)
- Backend container (Django + Gunicorn)
- MySQL container
- Health checks و networks

## 🚀 نحوه اجرا

### روی سرور (با Docker)

```bash
# 1. آپلود پروژه به سرور
cd /var/www
git clone <repository-url> ispp
cd ispp

# 2. اجرای Docker
docker-compose up -d --build

# 3. راه‌اندازی دیتابیس
docker-compose exec backend python manage.py migrate
docker-compose exec backend python manage.py createsuperuser
docker-compose exec backend python manage.py collectstatic --noinput

# 4. تست اتصال
chmod +x test-connection.sh
./test-connection.sh
```

### روی کامپیوتر شخصی (Development)

```bash
# Terminal 1: Backend
cd backend
python manage.py runserver

# Terminal 2: Frontend
cd frontend
npm run dev
```

## 🧪 تست اتصال

### روش 1: اسکریپت خودکار

```bash
./test-connection.sh
```

### روش 2: تست دستی

```bash
# تست Backend
curl http://localhost:8000/api/accounts/members/

# تست Frontend
curl http://localhost:80/api/accounts/members/
```

### روش 3: در مرورگر

1. باز کردن `http://localhost` یا `http://irpps.org`
2. رفتن به صفحه Login
3. وارد شدن با نام کاربری و رمز عبور
4. اگر وارد Dashboard شدید، همه چیز کار می‌کند! ✅

## 📊 معماری

```
مرورگر (Browser)
    ↓
Nginx (Port 80)
    ├─→ Static Files (HTML, CSS, JS)
    ├─→ /api/* → Backend
    └─→ /media/* → Backend
        ↓
Django Backend (Port 8000)
    ├─→ API Endpoints
    ├─→ Authentication
    └─→ Database Connection
        ↓
MySQL Database (Port 3306)
    └─→ Tables (Users, News, Events, ...)
```

## 🔐 Authentication

```
1. کاربر login می‌کند
2. Backend session ایجاد می‌کند
3. Cookie ذخیره می‌شود
4. درخواست‌های بعدی با cookie ارسال می‌شوند
5. Backend session را verify می‌کند
```

## 📁 فایل‌های مهم

- `DEPLOYMENT_GUIDE.md` - راهنمای کامل استقرار
- `TEST_CONNECTION.md` - راهنمای تست
- `INTEGRATION_COMPLETE.md` - جزئیات کامل
- `test-connection.sh` - اسکریپت تست
- `docker-compose.yaml` - تنظیمات Docker

## ✅ چک‌لیست

- [x] Frontend به Backend متصل شد
- [x] Backend به Database متصل شد
- [x] Authentication کار می‌کند
- [x] Dashboard کار می‌کند
- [x] Media files نمایش داده می‌شوند
- [x] CORS و CSRF صحیح هستند
- [x] Docker آماده است
- [x] Production ready است

## 🎉 تمام!

همه چیز آماده است. فقط کافیست:

1. پروژه را روی سرور آپلود کنید
2. `docker-compose up -d --build` را اجرا کنید
3. Migrations را اجرا کنید
4. تست کنید

**موفق باشید!** 🚀

---

برای جزئیات بیشتر، فایل `DEPLOYMENT_GUIDE.md` را مطالعه کنید.
