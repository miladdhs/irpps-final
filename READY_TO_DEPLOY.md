# آماده برای Deploy

## ✅ فایل Dashboard درست شد!

مشکل حل شد. فایل `frontend/src/views/Dashboard.vue` حالا کامل و بدون خطا هست.

## چیکار کنید؟

### 1. فایل‌ها رو به سرور بفرستید

```bash
# روی کامپیوتر محلی
git add .
git commit -m "Fix Dashboard with tabs"
git push

# روی سرور
cd /opt/irpps/src
git pull
```

### 2. بیلد کنید

```bash
# روی سرور
docker-compose down
docker-compose up -d --build
```

### 3. یوزر admin رو staff کنید

```bash
cd /opt/irpps/src/backend
chmod +x make_user_staff_server.sh
./make_user_staff_server.sh admin
```

### 4. تست کنید

1. از سایت خارج شوید
2. با یوزر `admin` وارد شوید  
3. به Dashboard بروید
4. باید این تب‌ها رو ببینید:
   - اطلاعات حساب (همه)
   - مدیریت اخبار (فقط admin)
   - اطلاعیه‌ها (فقط admin)
   - رویدادها (فقط admin)
   - اعضا (فقط admin)

## ساختار Dashboard

```
Dashboard
├── Header (عکس پروفایل، نام، خروج)
├── Tabs
│   ├── اطلاعات حساب (همه کاربران)
│   └── تب‌های مدیریتی (فقط admin)
│       ├── مدیریت اخبار
│       ├── اطلاعیه‌ها
│       ├── رویدادها
│       └── اعضا
└── محتوای تب فعال
```

## نکات مهم

✅ **کاربر عادی**: فقط تب "اطلاعات حساب" رو می‌بینه  
✅ **Admin**: همه تب‌ها رو می‌بینه  
✅ **امن**: تب‌ها با `v-if="authStore.isAdmin"` محافظت شدن  
✅ **سریع**: از iframe استفاده میکنه برای بارگذاری صفحات مدیریت  

## اگر مشکلی پیش اومد

### خطای بیلد:
```bash
# لاگ‌ها رو ببینید
docker logs irpps-frontend
docker logs irpps-backend
```

### تب‌های admin رو نمی‌بینم:
1. مطمئن شوید یوزر is_staff = True هست
2. از سایت خارج شوید و دوباره وارد شوید
3. Cache مرورگر رو پاک کنید

### بررسی is_staff:
```bash
cd /opt/irpps/src/backend
python3 manage.py shell
```
```python
from django.contrib.auth import get_user_model
User = get_user_model()
user = User.objects.get(username='admin')
print(f"is_staff: {user.is_staff}")
print(f"is_active: {user.is_active}")
```

موفق باشید! 🚀
