# 📚 سیستم پروفایل و رزومه اعضا

## 🎯 نحوه کار سیستم:

### 1. ورود کاربران
- **نام کاربری:** کد ملی
- **رمز عبور:** کد نظام پزشکی
- پس از ورود، کاربر به Dashboard هدایت می‌شود

### 2. Dashboard کاربری
کاربر می‌تواند:
- ✅ اطلاعات شخصی خود را مشاهده و ویرایش کند
- ✅ عکس پروفایل خود را آپلود/حذف کند
- ✅ رزومه خود را ویرایش کند شامل:
  - تحصیلات
  - مقالات و انتشارات
  - جوایز و افتخارات
  - گواهینامه‌ها
  - علایق پژوهشی
  - زبان‌ها
  - بیوگرافی

### 3. نمایش عمومی
- در صفحه "اعضا" (`/team`), لیست تمام اعضا نمایش داده می‌شود
- با کلیک روی هر عضو، Modal باز می‌شود و رزومه کامل نمایش داده می‌شود

---

## 🗄️ ساختار دیتابیس

### مدل CustomUser (accounts/models.py)

```python
class CustomUser(AbstractUser):
    # اطلاعات پایه
    phone = models.CharField(...)  # تلفن
    city = models.CharField(...)  # شهر
    specialty = models.CharField(...)  # تخصص
    experience = models.IntegerField(...)  # سابقه کار
    rating = models.DecimalField(...)  # امتیاز
    bio = models.TextField(...)  # بیوگرافی
    
    # عکس پروفایل
    profile_image = models.ImageField(upload_to='profile_images/')
    
    # رزومه
    education = models.TextField(...)  # تحصیلات
    publications = models.TextField(...)  # مقالات
    awards = models.TextField(...)  # جوایز
    certifications = models.TextField(...)  # گواهینامه‌ها
    research_interests = models.TextField(...)  # علایق پژوهشی
    languages = models.CharField(...)  # زبان‌ها
```

---

## 🔌 API Endpoints

### 1. دریافت پروفایل
```
GET /api/accounts/profile/
Authorization: Required (Login)
```

**Response:**
```json
{
  "success": true,
  "user": {
    "id": 1,
    "username": "1234567890",
    "first_name": "علی",
    "last_name": "احمدی",
    "email": "ali@example.com",
    "phone": "09123456789",
    "profile_image": "/media/profile_images/user1.jpg",
    "education": "دکترای تخصصی ریه کودکان",
    "publications": "10 مقاله در مجلات معتبر",
    "awards": "جایزه برتر پژوهش 1400",
    "certifications": "بورد تخصصی ریه",
    "research_interests": "آسم کودکان، آلرژی",
    "languages": "فارسی، انگلیسی"
  }
}
```

### 2. آپدیت اطلاعات شخصی
```
PUT /api/accounts/update-profile/
Authorization: Required
Content-Type: application/json
```

**Request Body:**
```json
{
  "first_name": "علی",
  "last_name": "احمدی",
  "email": "ali@example.com",
  "phone": "09123456789"
}
```

### 3. آپلود عکس پروفایل
```
POST /api/accounts/upload-profile-image/
Authorization: Required
Content-Type: multipart/form-data
```

**Form Data:**
```
profile_image: [File]
```

### 4. حذف عکس پروفایل
```
DELETE /api/accounts/delete-profile-image/
Authorization: Required
```

### 5. آپدیت رزومه
```
POST /api/accounts/update-resume/
Authorization: Required
Content-Type: application/json
```

**Request Body:**
```json
{
  "education": "دکترای تخصصی ریه کودکان از دانشگاه تهران",
  "publications": "- مقاله 1\n- مقاله 2\n- مقاله 3",
  "awards": "- جایزه برتر پژوهش 1400\n- جایزه نوآوری 1401",
  "certifications": "- بورد تخصصی ریه\n- فلوشیپ آلرژی",
  "research_interests": "آسم کودکان، آلرژی تنفسی، بیماری‌های مزمن ریوی",
  "languages": "فارسی، انگلیسی، عربی",
  "bio": "متخصص ریه کودکان با 15 سال سابقه"
}
```

### 6. لیست اعضا (عمومی)
```
GET /api/accounts/members/
Authorization: Not Required
```

**Response:**
```json
{
  "success": true,
  "members": [
    {
      "id": 1,
      "persian_name": "علی احمدی",
      "english_name": "Ali Ahmadi",
      "display_name": "علی احمدی",
      "email": "ali@example.com",
      "phone": "09123456789",
      "city": "تهران",
      "specialty": "ریه کودکان",
      "experience": 15,
      "rating": 4.8,
      "bio": "متخصص ریه کودکان",
      "profile_image": "/media/profile_images/user1.jpg",
      "education": "...",
      "publications": "...",
      "awards": "...",
      "certifications": "...",
      "research_interests": "...",
      "languages": "..."
    }
  ],
  "count": 50
}
```

---

## 🎨 صفحات Frontend

### 1. صفحه Dashboard (`/dashboard`)
**مسیر:** `frontend/src/views/Dashboard.vue`

**بخش‌ها:**
- Header با عکس پروفایل و نام کاربر
- Quick Actions (لینک به صفحات مختلف)
- بخش ویرایش پروفایل
- بخش ویرایش رزومه

### 2. صفحه اعضا (`/team`)
**مسیر:** `frontend/src/views/Team.vue`

**قابلیت‌ها:**
- ✅ نمایش لیست اعضا با عکس
- ✅ جستجو در اعضا
- ✅ کلیک روی عضو → باز شدن Modal با رزومه کامل
- ✅ نمایش تمام اطلاعات رزومه در Modal

---

## 🔄 جریان کار (Workflow)

### برای کاربر عادی:

1. **ورود:**
   ```
   کد ملی + کد نظام پزشکی → Login
   ```

2. **Dashboard:**
   ```
   مشاهده پروفایل → ویرایش اطلاعات → ذخیره
   ```

3. **آپلود عکس:**
   ```
   انتخاب فایل → آپلود → نمایش عکس جدید
   ```

4. **ویرایش رزومه:**
   ```
   پر کردن فرم‌ها → ذخیره → انتشار
   ```

5. **نمایش عمومی:**
   ```
   صفحه اعضا → کلیک روی نام → نمایش رزومه در Modal
   ```

### برای بازدیدکننده:

1. **مشاهده اعضا:**
   ```
   صفحه Team → لیست اعضا با عکس
   ```

2. **مشاهده رزومه:**
   ```
   کلیک روی عضو → Modal با اطلاعات کامل
   ```

---

## 📁 فایل‌های مرتبط

### Backend:
- `backend/accounts/models.py` - مدل CustomUser
- `backend/accounts/views.py` - API endpoints
- `backend/accounts/urls.py` - URL routing
- `backend/accounts/forms.py` - فرم‌های validation

### Frontend:
- `frontend/src/views/Dashboard.vue` - داشبورد کاربری
- `frontend/src/views/Team.vue` - صفحه اعضا
- `frontend/src/stores/auth.ts` - مدیریت authentication
- `frontend/src/utils/api.ts` - تنظیمات API

---

## 🔐 امنیت

### Authentication:
- تمام API های ویرایش نیاز به Login دارند
- Session-based authentication
- CSRF protection فعال

### Authorization:
- هر کاربر فقط می‌تواند پروفایل خودش را ویرایش کند
- لیست اعضا برای همه قابل مشاهده است
- رزومه‌ها عمومی هستند (بعد از انتشار توسط کاربر)

### File Upload:
- فقط فایل‌های تصویر مجاز هستند
- محدودیت حجم فایل
- ذخیره در `media/profile_images/`

---

## 🚀 نکات پیاده‌سازی

### 1. عکس پروفایل:
- اگر کاربر عکس نداره، placeholder نمایش داده می‌شود
- عکس‌ها در `/media/profile_images/` ذخیره می‌شوند
- Nginx باید `/media/` رو serve کنه

### 2. رزومه:
- فیلدهای رزومه `TextField` هستند (چند خطی)
- می‌تونن خالی باشن
- در Modal به صورت formatted نمایش داده می‌شوند

### 3. نام‌ها:
- `first_name` = نام فارسی
- `last_name` = نام انگلیسی
- `display_name` = نام نمایشی (ترجیحاً فارسی)

### 4. جستجو:
- جستجو در نام، تخصص، شهر
- Real-time search در frontend
- بدون نیاز به API call

---

## ✅ وضعیت فعلی

- ✅ Backend کامل و آماده
- ✅ API endpoints تست شده
- ✅ صفحه Team کار می‌کنه
- ✅ Modal نمایش رزومه کار می‌کنه
- ⚠️ Dashboard نیاز به آپدیت داره (فرم‌های ویرایش)

---

## 📝 TODO

1. آپدیت Dashboard.vue با فرم‌های ویرایش
2. اضافه کردن بخش آپلود عکس
3. اضافه کردن فرم ویرایش رزومه
4. تست کامل workflow
5. اضافه کردن validation در frontend
6. بهبود UI/UX

---

این سیستم کاملاً آماده است و فقط نیاز به اتصال Dashboard جدید به API های موجود داره! 🎉
