# راهنمای نهایی - Dashboard با تب‌های مدیریت

## چی تغییر کرد؟

Dashboard رو به یک صفحه تب‌دار تبدیل کردم:

### برای کاربر عادی:
- فقط تب "اطلاعات حساب" رو می‌بینه
- می‌تونه اطلاعات شخصی‌اش رو ویرایش کنه

### برای Admin (is_staff = True):
- تب "اطلاعات حساب" + 4 تب مدیریتی:
  - 📰 **مدیریت اخبار**
  - 📢 **اطلاعیه‌ها**
  - 📅 **رویدادها**
  - 👥 **اعضا**

## فایل‌های تغییر یافته

1. `frontend/src/views/Dashboard.vue` - Dashboard جدید با تب‌ها
2. `backend/news/views.py` - APIهای ویرایش و حذف اطلاعیه
3. `backend/news/urls.py` - URLهای جدید
4. `backend/events/views.py` - APIهای ویرایش و حذف رویداد
5. `backend/events/urls.py` - URLهای جدید
6. `frontend/src/views/admin/ManageNews.vue` - صفحه مدیریت اخبار
7. `frontend/src/views/admin/ManageAnnouncements.vue` - صفحه مدیریت اطلاعیه‌ها
8. `frontend/src/views/admin/ManageEvents.vue` - صفحه مدیریت رویدادها
9. `frontend/src/router/index.ts` - route اطلاعیه‌ها

## مراحل اجرا روی سرور

### 1. آپلود فایل‌ها

```bash
# با Git
git add .
git commit -m "Add tabbed dashboard with admin management"
git push

# روی سرور
cd /path/to/project
git pull
```

### 2. فعال‌سازی یوزر Admin

```bash
cd /path/to/project/backend
chmod +x make_user_staff_server.sh
./make_user_staff_server.sh admin
```

### 3. ری‌استارت سایت

```bash
# با Docker
docker-compose restart

# بدون Docker
cd frontend && npm run build
sudo systemctl restart your-django-service
```

### 4. تست

1. از سایت خارج شوید
2. با یوزر `admin` وارد شوید
3. به Dashboard بروید
4. باید 5 تب ببینید:
   - اطلاعات حساب
   - مدیریت اخبار
   - اطلاعیه‌ها
   - رویدادها
   - اعضا

## نکات مهم

✅ **ساده و تمیز**: همه چی تو یک صفحه Dashboard  
✅ **امن**: تب‌های مدیریتی فقط برای is_staff نمایش داده میشن  
✅ **سریع**: از iframe استفاده میکنه برای بارگذاری صفحات مدیریت  

❌ **کاربر عادی**: فقط تب "اطلاعات حساب" رو می‌بینه  
✅ **Admin**: همه تب‌ها رو می‌بینه و می‌تونه محتوا رو مدیریت کنه  

## عیب‌یابی

### تب‌های مدیریتی رو نمی‌بینم:
1. مطمئن شوید یوزر is_staff = True هست
2. از سایت خارج شوید و دوباره وارد شوید
3. Cache مرورگر رو پاک کنید

### iframe خالی هست:
1. مطمئن شوید frontend بیلد شده
2. مطمئن شوید routeها درست تنظیم شدن
3. Console مرورگر رو چک کنید

موفق باشید! 🎉
