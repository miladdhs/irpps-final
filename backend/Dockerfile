###################### Backend ?? ???? Python Slim ######################
FROM python:3.9-slim

WORKDIR /app

# ? ??? ???? ??????? sed (??????? ?? ????? ???? ????? ???? APT)
# ??? ??????????? ?????
RUN apt-get update && apt-get install --fix-missing -y \
    gcc \
    pkg-config \
    default-libmysqlclient-dev \
    libssl-dev \
    libffi-dev \
    python3-dev \
    && rm -rf /var/lib/apt/lists/*

# ? ????? pip ?? ????? ?????? (??? ??? ?? ??? ???? ?? ???? ??????)
RUN pip config set global.index-url https://mirror-pypi.runflare.com/simple \
    && pip config set global.trusted-host mirror-pypi.runflare.com \
    && pip config set global.timeout 60

# ??? ???????? ??????
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# ??? ?? ?????
COPY . .

# ????? ??????? entrypoint
COPY docker-entrypoint.sh /app/docker-entrypoint.sh
RUN chmod +x /app/docker-entrypoint.sh

# ???????? ?????
ENV PYTHONUNBUFFERED=1 \
    IS_DOCKER=True \
    DJANGO_SETTINGS_MODULE=ispp_project.settings

# ???? ????????????? ???? ????
RUN mkdir -p /app/staticfiles /app/media /app/media/profile_images /app/logs

EXPOSE 8000
ENTRYPOINT ["/app/docker-entrypoint.sh"]
