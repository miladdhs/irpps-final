# راهنمای اجرای پروژه ISPP

## پیش‌نیازها

- ✅ Docker Desktop (نصب شده)
- ✅ Python 3.9+ (برای Backend)
- ✅ Node.js 16+ (برای Frontend)

## روش 1: اجرای سریع با اسکریپت‌ها (توصیه میشه)

### مرحله 1: اجرای Docker و MySQL

```powershell
.\start-dev.ps1
```

این اسکریپت:
- Docker رو چک میکنه
- MySQL رو با Docker اجرا میکنه
- راهنمای اجرای Backend و Frontend رو نشون میده

### مرحله 2: اجرای Backend

**ترمینال جدید باز کن** و این دستور رو اجرا کن:

```powershell
cd backend
.\start-backend.ps1
```

این اسکریپت:
- Python رو چک میکنه
- Virtual environment میسازه
- Dependencies رو نصب میکنه
- Migrations رو اجرا میکنه
- Django server رو اجرا میکنه

### مرحله 3: اجرای Frontend

**ترمینال جدید دیگه باز کن** و این دستور رو اجرا کن:

```powershell
cd frontend
.\start-frontend.ps1
```

این اسکریپت:
- Node.js رو چک میکنه
- Dependencies رو نصب میکنه
- Vite dev server رو اجرا میکنه

---

## روش 2: اجرای دستی (مرحله به مرحله)

### مرحله 1: اجرای Docker Desktop

1. از Start Menu برو به **Docker Desktop**
2. منتظر بمون تا آیکون Docker در System Tray **سبز** بشه
3. تست کن:
   ```powershell
   docker ps
   ```

### مرحله 2: اجرای MySQL

```powershell
docker-compose up -d mysql
```

چک کردن وضعیت:
```powershell
docker-compose ps
docker-compose logs -f mysql
```

### مرحله 3: اجرای Backend

```powershell
cd backend

# ایجاد virtual environment (فقط بار اول)
python -m venv venv

# فعال‌سازی virtual environment
.\venv\Scripts\Activate.ps1

# نصب dependencies (فقط بار اول یا بعد از تغییر requirements.txt)
pip install -r requirements.txt

# اجرای migrations
python manage.py migrate

# ایجاد superuser (اختیاری - فقط بار اول)
python manage.py createsuperuser

# اجرای سرور
python manage.py runserver 0.0.0.0:8000
```

### مرحله 4: اجرای Frontend

**ترمینال جدید باز کن:**

```powershell
cd frontend

# نصب dependencies (فقط بار اول یا بعد از تغییر package.json)
npm install

# اجرای dev server
npm run dev
```

---

## آدرس‌های دسترسی

بعد از اجرای همه سرویس‌ها:

- 🌐 **Frontend**: http://localhost:5174
- 🔧 **Backend API**: http://localhost:8000
- 👨‍💼 **Admin Panel**: http://localhost:8000/admin
- 🗄️ **MySQL**: localhost:3306

---

## دستورات مفید

### Docker

```powershell
# مشاهده کانتینرهای در حال اجرا
docker-compose ps

# مشاهده لاگ‌ها
docker-compose logs -f mysql

# متوقف کردن MySQL
docker-compose down

# ری‌استارت MySQL
docker-compose restart mysql

# اجرای دستور در MySQL
docker-compose exec mysql mysql -u irporg_admin -p irporg_DB
# Password: tHPXArRfwrX3WH!*j
```

### Backend (Django)

```powershell
cd backend

# ایجاد migration جدید
python manage.py makemigrations

# اجرای migrations
python manage.py migrate

# ایجاد superuser
python manage.py createsuperuser

# باز کردن Django shell
python manage.py shell

# جمع‌آوری static files
python manage.py collectstatic

# چک کردن مشکلات
python manage.py check
```

### Frontend (Vue + Vite)

```powershell
cd frontend

# نصب یک package جدید
npm install package-name

# Build برای production
npm run build

# Preview build
npm run preview

# چک کردن مشکلات
npm run lint
```

---

## اتصال به MySQL از خارج Docker

اگر میخوای با MySQL Workbench یا ابزار دیگه‌ای وصل بشی:

### 1. فعال کردن پورت MySQL

در فایل `docker-compose.yaml`، این خطوط رو uncomment کن:

```yaml
mysql:
  ports:
    - "3306:3306"
```

### 2. ری‌استارت MySQL

```powershell
docker-compose restart mysql
```

### 3. اطلاعات اتصال

- **Host**: `localhost`
- **Port**: `3306`
- **User**: `irporg_admin`
- **Password**: `tHPXArRfwrX3WH!*j`
- **Database**: `irporg_DB`

---

## حل مشکلات رایج

### مشکل 1: Docker Desktop اجرا نیست

**خطا**: `error during connect: ... cannot find the file specified`

**راه حل**:
1. Docker Desktop رو از Start Menu اجرا کن
2. منتظر بمون تا آیکون سبز بشه
3. دوباره تلاش کن

### مشکل 2: پورت 8000 یا 5174 در حال استفاده است

**راه حل**:
```powershell
# پیدا کردن process که پورت رو استفاده میکنه
netstat -ano | findstr :8000
netstat -ano | findstr :5174

# کشتن process (با PID که پیدا کردی)
taskkill /PID <PID> /F
```

### مشکل 3: خطای اتصال به MySQL

**راه حل**:
1. چک کن MySQL اجرا باشه:
   ```powershell
   docker-compose ps mysql
   ```
2. اگر اجرا نیست:
   ```powershell
   docker-compose up -d mysql
   ```
3. منتظر بمون 10-15 ثانیه تا MySQL آماده بشه

### مشکل 4: خطای Migration

**راه حل**:
```powershell
cd backend
python manage.py migrate --fake-initial
```

### مشکل 5: خطای CORS در Frontend

**راه حل**:
- مطمئن شو Backend روی `http://localhost:8000` اجرا باشه
- فایل `.env` در frontend رو چک کن:
  ```
  VITE_API_URL=http://localhost:8000/api
  ```

---

## توقف سرویس‌ها

### توقف Backend
در ترمینال Backend: `Ctrl + C`

### توقف Frontend
در ترمینال Frontend: `Ctrl + C`

### توقف MySQL
```powershell
docker-compose down
```

---

## نکات مهم

1. ✅ **فایل‌های Docker تغییر نکردن** - همون فایل‌های قبلی استفاده میشن
2. ✅ **دیتابیس حفظ میشه** - داده‌های موجود در `mysql/` دست نخورده باقی میمونن
3. ✅ **برای Development** - بهتره فقط MySQL رو با Docker اجرا کنی و Backend/Frontend رو local
4. ✅ **برای Production** - همه سرویس‌ها رو با Docker اجرا کن: `docker-compose up -d`

---

## تست کردن

بعد از اجرای همه سرویس‌ها:

1. برو به http://localhost:5174
2. صفحه اصلی سایت باید لود بشه
3. برو به http://localhost:8000/admin
4. با superuser که ساختی لاگین کن
5. برو به http://localhost:5174/login
6. با همون superuser لاگین کن

---

## پشتیبانی

اگر مشکلی پیش اومد:
1. لاگ‌های MySQL رو چک کن: `docker-compose logs -f mysql`
2. لاگ‌های Backend رو چک کن (در ترمینال Backend)
3. لاگ‌های Frontend رو چک کن (در ترمینال Frontend)
4. Console مرورگر رو چک کن (F12)
